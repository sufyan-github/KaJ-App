# @kaj/shared-types

This workspace will contain API DTOs and enums shared by the NestJS backend and Next.js admin app.
Domain types are intentionally not defined during P1-INF-01; their owning tasks add them from the
authoritative schema.
